apiVersion: v1
kind: Service
metadata:
  name: localstack
  namespace: signaltrade
  labels:
    app.kubernetes.io/name: localstack
    app.kubernetes.io/part-of: signaltrade
spec:
  selector:
    app.kubernetes.io/name: localstack
  ports:
    - name: edge
      port: 4566
      targetPort: edge
